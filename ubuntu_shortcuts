#terminal
	- pwd : 현재위치(Present Working Directory)
	- ls : 파일 표시
		ls -l : 한줄씩 나열 + 세부사항표시
		ls -a : 숨긴 파일 표시
	- cd ~ : ~디텍토리로 이동  ( 디렉토리 = windows 에서는 폴더 ) (Change Directory)
		cd : 초기디렉토리로 이동
		cd .. : 상위 디렉토리로 이동 ( . : 현재 디렉토리 , .. : 상위디렉토리 )
	- mkdir ~ : ~디렉토리 생성 (Make Directory)
	- rm ~ : ~파일 삭제 (Remove)
		rmdir ~ : ~디렉토리 삭제 (Remove Directory)
	- vi ~ : a screen-oriented text editor originally created for the Unix operating system
		(vim을 따로 설치하길 권장 - vim을 설치하고  vi 사용하면 편함)
		use) vi helloworld.c
	- Compile : GCC로 컴파일함. 
		use) gcc -o helloworld helloworld.c  --> helloworld라는 파일 생성
			-o : outfile(실행프로그램) 만들기 ( 컴파일하는 동안 임시생성된 파일은 다 지워짐 )
			-s : 컴파일 중 assembly code 파일을 만듬 --> helloworld.s 라는 파일 생성
				gcc -s helloworld.c
			-v : 컴파일되는 모든 과정을 보여줌
			-Wall : 최대한 경고를 많이 준다.
			-g : 디버깅(dgb) 할때 list 기능 사용 가능하도록
	- gedit ~ : txt editor file 생성
	- 'Tab' 키 : 자동완성기능
	- clear : 화면 지우기 
		==(Ctrl+l)
	- exit : 터미널 쉘 종료
		==(Ctrl+d)
	- Ctrl+(+) : 글자 크기 크게
		-  : 	      작게
	- sudo : super user do!
		the one command to rule them all.
	- apt-get : a powerful and free package management command line program
		(Advanced Packaging Tool)
		use) sudo apt-get install tmux
			password : (숫자를 입력해도 표시되지는 않음-그냥 쳐야함)
			==>(tmux설치)
	- F11 : 전체 화면 --> command키 + Ctrl + 위방향키 
	- locate : file위치 찾기
	- mv : 파일이동, 이름변경
		mv ./test.cc /home/leehyeonmin/softi/ : 현재디렉토리의 test.cc를 softi로 이동
		mv ./test.cc ./test_rename.cc : 현재디렉토리의 test.cc의 이름을 test_rename.cc로 변경
		mv ./test.cc /home/leehyeonmin/softi/test_rename.cc : 현재디렉토리의 test.cc를 softi로 이동한후 이름을 test_rename.cc로 변경

#Vim
	- i : 입력모드
	- :w : 저장
	- :q : 종료
	- :q! : 강제종료
	- :wq : 저장 후 종료
		==(ZZ)
	- dd : 커서가 있는 행 지우기
	- yy : 그 행 복사
		[N]yy : 그 행부터 아래로 N 줄까지 복사
	- p : 커서가 있는 행 아래로 복사한 행 붙여넣기
		[N]p : 복사한 행을 N번 복사	
	- u : 방금 전 수행한 명령 취소  ( 다른프로그램에선 주로 Ctrl+z )
	- 행(줄) 번호 나타내기
		-임시적용 : :se nu, :set nu
		-영구적용 : /etc/virc의 맨 아래에 set nu 추가
			cd /etc  or  cd /etc/vim
			--> sudo vi virc
			--> Input password 
			--> 마지막 부분에 set nu 추가 후 저장
	- [N] + (Shift+g) : N번째 줄로 이동

#Tmux
	- Ctrl + b 누른 후 그 다음
			+ % : 오른쪽에 새로운 셀 생성
			+ " : 아래에 새로운 셀 생성
			+ t : 현재 셀에 시간 표시
			+ 방향키 : 그 방향의 셀로 이동
			+ x : (y/n) y: 셀 종료     (Ctrl+d가 더 빠름)
	- Ctrl + b 누른 채로 방향키 : 셀을 나눈 선의 위치 이동 
	
	
